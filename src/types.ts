export interface Bird {
  id: string;
  name: string;
  englishName?: string;
  scientificName: string;
  category: string;
  height: string;
  weight: string;
  description: string;
  imageUrl: string;
  habitat: string;
  diet: string;
  rarity: string;
  discovered: boolean;
}

// 如果你還有其他型別定義，接在下面即可
